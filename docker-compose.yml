services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio_web:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M

